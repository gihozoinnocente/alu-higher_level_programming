#!/usr/bin/node

const argsLength = process.argv.length

if ( argsLenght === 2 ){
	console.log( 'No argument');
} if else ( argsLength === 3) {
	console.log( 'Argument found');
} else {
	console.log ( 'Arguments found');
}
